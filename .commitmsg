fix(cli): address Copilot review — extract error helper, fix debug gate, add test

- Remove bogus argv.includes('--debug') check (no --debug CLI flag
  exists); gate stack traces on GETDOTENV_DEBUG env var only.
- Extract error boundary into handleCliError() helper to reduce
  createCli module size.
- Add test for AWS dynamic import failure (missing optional peer dep).
- Remove ephemeral .commitmsg from tracked files.
