name: Deploy Docs

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write
  packages: read

jobs:
  docs:
    uses: karmaniverous/gha-workflows/.github/workflows/docs.yml@main
