/**
 * @packageDocumentation
 * CLI factory for composing the get-dotenv host with shipped plugins.
 * Import from `@karmaniverous/get-dotenv/cli` to embed the CLI in your own tools
 * or to build a custom host with a curated set of plugins.
 */

export { createCli, type CreateCliOptions } from './createCli';
