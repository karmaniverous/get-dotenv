/**
 * @packageDocumentation
 * Config loader and validation helpers used by the host. Exposes utilities
 * to discover, load, and validate get-dotenv configuration documents from
 * JSON/YAML/JS/TS sources.
 */

export * from './loader';
export * from './validate';
